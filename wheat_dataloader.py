import torch
import cv2
from torch.utils.data import Dataset

class wheat_dataset(torch.utils.data.Dataset):
    def __init__(self, transform):
    

    def __getitem__(self, index):

        return image, target, image_idx

    def __len__(self):

        return 0